import 'package:flutter/material.dart';

class AppColors {
  //🌞Light Theme colors
  static const Color background = Color(0xFFF5F9FF);
  static const Color primaryBlue = Color(0xFF3B82F6);
  static const Color Grey = Color(0xFFE0E0E0);
  static const Color textDark = Colors.black87;

  // Dark theme colors
  static const Color darkBackground = Color(0xFF0F172A); // background
  static const Color darkPrimaryBlue = Color(0xFFBCD3FF); // visitor button
  static const Color darkAdmin = Color(0xFF292929);
  static const Color textLight = Colors.white;
}
